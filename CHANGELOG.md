Trait CanLoadRelationships

Authentication using Sanctum

Token support

Logout support and token expiration

Auth policies, gates are not used hence the AuthServiceProvider is rendered pratically useless. It only exists because it was used before, continuing forward it policies will be used unless the Gate requirement is gonna pop up

Added email notification reminders with schedule set up for them to run daily
